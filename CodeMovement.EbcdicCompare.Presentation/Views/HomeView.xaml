<UserControl x:Class="Wcb.Eco.EbcdicCompare.Ui.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="320" d:DesignWidth="320">
    <Grid x:Name="LayoutRoot" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Button Command="{Binding OpenEbcdicFileCommand}" Grid.Column="0" Grid.Row="0" 
                Height="50" Width="300"
                AutomationProperties.AutomationId="OpenEbcdicFileButton">View EBCDIC File</Button>
        <Button Command="{Binding CompareEbcdicFilesCommand}" Grid.Column="0" Grid.Row="1" 
                Height="50" Width="300"
                AutomationProperties.AutomationId="CompareEbcdicFilesButton">Compare EBCDIC Files</Button>
        <Button Command="{Binding ManageCopybookXmlFilesCommand}" Grid.Column="0" Grid.Row="2" 
                Height="50" Width="300"
                AutomationProperties.AutomationId="ManageCopybooksViewButton">Manage Copybook XML Files</Button>
    </Grid>
</UserControl>
